<?xml version="1.0" encoding="utf-8"?>
<!--
  Izin yang diminta sengaja SATU: internet.

  Aplikasi ujian yang meminta kamera, mikrofon, lokasi, dan daftar aplikasi
  terpasang akan ditolak sebagian peserta dengan alasan yang benar. Yang
  dikerjakan aplikasi ini hanya membuka satu halaman web dengan satu bendera
  keamanan; ia tidak membaca apa pun dari ponsel pemakainya, dan izin yang
  tidak diminta adalah janji yang paling mudah dibuktikan.

  Pengawasan kamera pada ujian sertifikasi tetap berjalan lewat izin kamera
  yang diminta HALAMAN webnya sendiri, dan peserta memberikannya di sana —
  sekali, untuk ujian yang memang menyalakannya.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_lock_lock"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.SiPalingLockdown">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="fullUser"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:launchMode="singleTask"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
